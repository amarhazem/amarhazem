name: ah
networks:
  network:
services:
  pgadmin4:
    extends:
      file: ../../services/pgadmin4/docker-compose.yaml
      service: pgadmin4
  postgres:
    extends:
      file: ../../services/postgres/docker-compose.yaml
      service: postgres
volumes:
  pgadmin4:
  postgres:
